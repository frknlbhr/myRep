@using MySimpleMvcProject.Models
@model Student

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @using (Html.BeginForm("Add", "Student", FormMethod.Post))
        {
            @Html.TextBoxFor(c => c.Name)
            @Html.TextBoxFor(m => m.Surname)
            <input type="submit" value="Add" />
        }
    </p>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegisterDate)
            </th>
            <th></th>
        </tr>

        @{
            var myStdList = (List<Student>)ViewData["StdList"];
        }
    
    @foreach (var item in myStdList) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegisterDate)
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", item)
            </td>
        </tr>
    }
    
    </table>
</body>
</html>

<script type="text/javascript">
//i'm using jquery ready event which will call the javascript chunk after the page has completed loading
$(document).ready(function(){
var dSuccess = '@StudentList.isDeleted'.toLowerCase;
if(dSuccess == 'true'){
    alert("Successfully Deleted");
}
});

</script>
<script type="text/javascript">
    function showAlert()
    { alert('asd'); }
</script>
